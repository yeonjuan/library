{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.2.1",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "format": "prettier --write .",
    "check:format": "prettier --list-different .",
    "check:ts": "tsc --noEmit",
    "build": "lerna run build"
  }
}
